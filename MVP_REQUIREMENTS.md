# 图片背景移除 MVP 需求文档

## 1. 项目概述

### 1.1 项目名称
**Image Background Remover（图片背景移除服务）**

### 1.2 项目简介
一个基于 Web 的图片背景自动移除服务，用户上传图片后，系统自动识别并移除背景，返回透明背景的 PNG 图片。服务部署在 Cloudflare Workers，使用 remove.bg API 进行处理。

### 1.3 项目目标
- **核心目标**：提供简单、快速、免费的图片背景移除服务
- **商业目标**：验证市场需求，收集用户反馈，为后续商业化做准备
- **技术目标**：建立稳定可扩展的基础架构

### 1.4 目标用户
- 电商卖家（产品图背景处理）
- 设计师（快速抠图）
- 内容创作者（社交媒体配图）
- 普通用户（头像、证件照背景处理）

---

## 2. MVP 范围

### 2.1 功能范围（MVP）

#### 核心功能
✅ **图片上传**
- 支持点击上传
- 支持拖拽上传
- 支持 JPG、PNG、WEBP 格式
- 图片大小限制：最大 12MB（remove.bg 限制）

✅ **背景移除**
- 自动调用 remove.bg API
- 返回透明背景的 PNG 图片
- 实时处理状态显示

✅ **结果预览与下载**
- 处理完成后显示预览图
- 一键下载处理后的图片
- 文件名自动添加时间戳

✅ **错误处理**
- 文件格式错误提示
- 文件过大提示
- API 调用失败提示
- 网络错误提示

### 2.2 非功能需求（MVP）

#### 性能要求
- 响应时间：< 5 秒（从上传到完成）
- 并发支持：至少 10 个用户同时使用
- 可用性：99%（Cloudflare Workers SLA）

#### 安全要求
- API Key 加密存储（Cloudflare Secrets）
- HTTPS 传输（强制）
- 无数据持久化（内存处理）
- CORS 配置（防止滥用）

#### 用户体验要求
- 简洁直观的界面
- 清晰的操作指引
- 实时状态反馈
- 移动端适配

### 2.3 功能范围（不在 MVP）

❌ 用户注册/登录
❌ 批量处理
❌ 图片编辑工具
❌ 历史记录
❌ 支付功能
❌ API 密钥管理
❌ 自定义处理参数（边缘羽化、色彩调整等）
❌ 多语言支持（仅中文）

---

## 3. 技术架构

### 3.1 技术栈

#### 前端
- **框架**：原生 JavaScript（无框架）
- **样式**：原生 CSS（响应式设计）
- **特性**：File API、Fetch API、Drag & Drop API

#### 后端
- **平台**：Cloudflare Workers
- **语言**：JavaScript（ES6+）
- **部署**：Wrangler CLI

#### 第三方服务
- **API**：remove.bg API v1.0
- **套餐**：Free Tier（50 张/月）

#### 域名与托管
- **域名**：`bg-remover.workers.dev`（免费）
- **HTTPS**：Cloudflare 自动提供
- **CDN**：Cloudflare 全球网络

### 3.2 系统架构图

```
用户浏览器
    │
    │ 1. 上传图片
    ▼
Cloudflare Workers（边缘节点）
    │
    │ 2. 接收图片（内存）
    ▼
remove.bg API
    │
    │ 3. 处理图片
    ▼
Workers（返回 PNG）
    │
    │ 4. 下载/预览
    ▼
用户浏览器
```

### 3.3 数据流

```
上传 → 验证 → 调用 API → 处理 → 返回 → 下载
```

**无存储设计**：所有处理在内存中完成，不保存任何图片数据。

---

## 4. 用户流程

### 4.1 主要用户流程

```
1. 用户访问网站
   ↓
2. 看到"上传图片"区域
   ↓
3. 选择上传方式：
   - 点击上传
   - 拖拽上传
   ↓
4. 系统验证图片（格式、大小）
   ↓
5. 点击"移除背景"按钮
   ↓
6. 显示"正在处理中..."
   ↓
7. 接收处理结果（2-5秒）
   ↓
8. 显示预览图
   ↓
9. 用户可以：
   - 下载图片
   - 重新上传
```

### 4.2 异常流程

```
1. 上传非图片文件
   ↓
   显示错误："请上传图片文件"

2. 图片超过 12MB
   ↓
   显示错误："图片过大（最大 12MB）"

3. API 调用失败
   ↓
   显示错误："处理失败，请稍后重试"

4. 网络错误
   ↓
   显示错误："网络连接失败，请检查网络"
```

---

## 5. 界面设计（MVP）

### 5.1 页面布局

```
┌─────────────────────────────────────┐
│      🖼️ 图片背景移除                 │
├─────────────────────────────────────┤
│                                     │
│   ┌─────────────────────────────┐   │
│   │                             │   │
│   │    点击或拖拽上传图片         │   │
│   │                             │   │
│   └─────────────────────────────┘   │
│                                     │
│      [ 移除背景 ]                    │
│                                     │
│   正在处理中... (隐藏)               │
│                                     │
│   错误信息 (隐藏)                    │
│                                     │
│   ┌─────────────────────────────┐   │
│   │   处理结果：                 │   │
│   │   [ 预览图 ]                 │   │
│   │   [ 下载图片 ]               │   │
│   └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

### 5.2 交互状态

| 状态 | 显示内容 |
|------|---------|
| 初始 | 上传区域 + 按钮（禁用） |
| 已选择文件 | 上传区域 + 按钮（启用） |
| 处理中 | 上传区域 + 按钮（禁用）+ "正在处理中..." |
| 成功 | 上传区域 + 按钮 + 预览图 + 下载按钮 |
| 错误 | 上传区域 + 按钮 + 错误信息 |

### 5.3 响应式设计

- **桌面端**：最大宽度 800px，居中显示
- **移动端**：100% 宽度，触摸友好
- **平板端**：自适应布局

---

## 6. API 设计

### 6.1 API 端点

#### POST `/api/remove-bg`

**请求：**

```
Content-Type: multipart/form-data

file: [图片文件]
```

**成功响应：**

```
Status: 200 OK
Content-Type: image/png

[PNG 图片二进制数据]
```

**错误响应：**

```
Status: 400 Bad Request / 500 Internal Server Error
Content-Type: application/json

{
  "error": "错误信息"
}
```

### 6.2 错误码

| HTTP 状态码 | 说明 |
|-----------|------|
| 200 | 成功 |
| 400 | 请求错误（文件过大、格式错误） |
| 401 | API Key 无效 |
| 429 | API 额度用尽 |
| 500 | 服务器错误 |

---

## 7. 部署方案

### 7.1 开发环境

```bash
# 1. 安装依赖
npm install

# 2. 配置环境变量
echo "REMOVE_BG_API_KEY=your-key" > .dev.vars

# 3. 启动本地开发服务器
wrangler dev
```

访问：`http://localhost:8787`

### 7.2 生产环境

```bash
# 1. 登录 Cloudflare
wrangler login

# 2. 设置 Secrets
wrangler secret put REMOVE_BG_API_KEY

# 3. 部署
wrangler deploy
```

部署后获得域名：`https://bg-remover.workers.dev`

### 7.3 域名绑定（可选）

```toml
# wrangler.toml
routes = [
  { pattern = "your-domain.com/*", zone_name = "your-domain.com" }
]
```

---

## 8. 免费额度与限制

### 8.1 Cloudflare Workers 免费额度

- 请求次数：100,000 次/天
- CPU 时间：10ms/请求
- 全球边缘节点：自动分配

**评估**：足够 MVP 使用

### 8.2 remove.bg 免费额度

- 处理次数：50 张/月
- 图片大小：最大 12MB
- 响应时间：2-5 秒

**限制**：
- ❌ 仅 50 张/月，远远不够
- ❌ 无商业许可
- ❌ 受速率限制

### 8.3 MVP 应对策略

**短期（MVP）**：
- 使用 remove.bg 免费额度
- 控制访问量
- 添加使用提示（"今日剩余：X 次"）

**中期**：
- 升级到付费套餐（500 张/月 ≈ $9）
- 或切换到开源模型（RMBG-1.4）

**长期**：
- 自建推理服务
- 多 API 混合使用
- 用户付费订阅

---

## 9. 成功指标（MVP）

### 9.1 技术指标

| 指标 | 目标 |
|-----|------|
| 可用性 | > 99% |
| 响应时间 | < 5 秒 |
| 成功率 | > 95% |
| 错误率 | < 5% |

### 9.2 产品指标

| 指标 | 目标（30 天） |
|-----|-------------|
| 访问量 | > 1,000 |
| 处理图片 | > 100 |
| 回访率 | > 20% |
| 分享率 | > 10% |

### 9.3 用户反馈指标

| 指标 | 目标 |
|-----|------|
| NPS 评分 | > 30 |
| Bug 报告 | < 10 |
| 功能建议 | > 20 |

---

## 10. 风险评估

### 10.1 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|-----|------|------|---------|
| remove.bg API 宕机 | 高 | 低 | 准备备用 API（RMBG-1.4） |
| API 额度用尽 | 高 | 高 | 升级付费套餐或切换开源模型 |
| Cloudflare Workers 限制 | 中 | 低 | 优化代码，减少 CPU 时间 |
| 图片处理失败 | 中 | 中 | 完善错误处理和重试机制 |

### 10.2 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|-----|------|------|---------|
| 用户需求不足 | 高 | 中 | 快速验证，收集反馈 |
| 成本超出预期 | 中 | 中 | 严格监控 API 调用量 |
| 竞品压力 | 中 | 中 | 强调简单、免费、快速 |

### 10.3 法律风险

| 风险 | 影响 | 概率 | 应对措施 |
|-----|------|------|---------|
| 用户上传侵权图片 | 中 | 低 | 添加服务条款，免责声明 |
| 隐私数据泄露 | 高 | 低 | 无存储设计，HTTPS 传输 |
| API 许可合规 | 中 | 低 | 检查 remove.bg 服务条款 |

---

## 11. 开发计划（MVP）

### 11.1 阶段划分

#### 阶段 1：开发（3-5 天）

**任务列表：**
- [x] 创建项目结构
- [x] 编写 Workers 代码
- [x] 编写前端 HTML/CSS/JS
- [x] 配置 wrangler.toml
- [ ] 本地测试
- [ ] 部署到 Cloudflare
- [ ] 配置环境变量

**负责人**：开发者
**验收标准**：可正常部署并访问

#### 阶段 2：测试（2-3 天）

**测试项目：**
- [ ] 功能测试（上传、处理、下载）
- [ ] 兼容性测试（Chrome、Firefox、Safari、Edge）
- [ ] 响应式测试（桌面、平板、手机）
- [ ] 边界测试（大文件、非图片、空文件）
- [ ] 性能测试（并发、响应时间）
- [ ] 安全测试（CORS、HTTPS）

**负责人**：测试人员
**验收标准**：无 P0/P1 Bug

#### 阶段 3：发布（1 天）

**发布步骤：**
- [ ] 绑定自定义域名（可选）
- [ ] 配置监控和告警
- [ ] 准备发布文案
- [ ] 通知种子用户
- [ ] 收集用户反馈

**负责人**：产品经理
**验收标准**：成功上线，用户可访问

#### 阶段 4：迭代（持续）

**优化方向：**
- [ ] 添加使用次数显示
- [ ] 优化错误提示
- [ ] 添加 loading 动画
- [ ] 移动端体验优化
- [ ] 添加分享功能

**负责人**：产品 + 开发
**验收标准**：用户满意度提升

### 11.2 时间线

```
Week 1（开发）
Day 1-2: 后端开发
Day 3-4: 前端开发
Day 5: 联调 + 部署

Week 2（测试 + 发布）
Day 1-2: 测试
Day 3: 修复 Bug
Day 4: 发布
Day 5: 收集反馈
```

---

## 12. 后续规划（MVP 之后）

### 12.1 功能扩展

**P0 优先级：**
- 用户注册/登录
- 使用次数限制（每日免费 3 次）
- API 额度升级提示
- 多语言支持（英文、日文）

**P1 优先级：**
- 批量处理
- 历史记录
- 图片编辑（调整大小、裁剪）
- API 调用监控

**P2 优先级：**
- 高级参数（边缘羽化、色彩调整）
- 多模型选择
- 图片格式转换
- 短视频背景移除

### 12.2 技术升级

- 切换到开源模型（RMBG-1.4）
- 自建推理服务（降低成本）
- 添加 KV 缓存（减少 API 调用）
- 多 API 混合使用（降低风险）

### 12.3 商业化

- 付费订阅（无限次使用）
- API 服务（企业用户）
- 私有化部署（大型客户）
- 白标解决方案（合作伙伴）

---

## 13. 附录

### 13.1 参考资料

- **Cloudflare Workers 文档**：https://developers.cloudflare.com/workers/
- **remove.bg API 文档**：https://www.remove.bg/zh/api
- **Wrangler CLI 文档**：https://developers.cloudflare.com/workers/wrangler/

### 13.2 技术术语

- **Cloudflare Workers**：Cloudflare 的边缘计算平台
- **remove.bg API**：商业背景移除 API
- **Wrangler**：Cloudflare Workers 的官方 CLI 工具
- **CORS**：跨域资源共享
- **MVP**：最小可行产品（Minimum Viable Product）

### 13.3 联系方式

- **项目负责人**：Joka
- **技术负责人**：开发者
- **产品负责人**：产品经理

---

**文档版本**：v1.0
**创建日期**：2026-03-19
**最后更新**：2026-03-19
**状态**：MVP 需求已确认
